import React from 'react';
import Reservation from './Reservation';

const ReservationList = () => (
  <ul className="list-unstyled fs-4 fw-bold text-green">
    <Reservation />
  </ul>
);

export default ReservationList;
